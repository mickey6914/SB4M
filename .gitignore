node_modules/
dist/
.env
.env.*
!.env.example
*.log
# Anchored patterns containing a slash match from this file's directory, so
# the bare form missed server/scripts/out/ — and let one generated backdrop
# into the history.
**/scripts/out/
